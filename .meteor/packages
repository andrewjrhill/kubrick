# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.

meteor-base@1.0.4                # Packages every Meteor app needs to have.
mobile-experience@1.0.4          # Packages for a great mobile UX.
mongo@1.1.14                     # The database Meteor supports right now.
blaze-html-templates@1.0.4       # Compile .html files into Meteor Blaze views.
reactive-var@1.0.11              # Reactive variable for tracker.
reactive-dict@1.1.8              # Meteor reactive dictionaries.
jquery@1.11.10                   # Helpful client-side library.
tracker@1.1.1                    # Meteor's client-side reactive programming library.
shell-server@0.2.1               # Server-side component of the `meteor shell` command.
percolate:migrations             # Define and run db migrations.

standard-minifier-css@1.3.2      # CSS minifier run for production mode.
standard-minifier-js@1.2.1       # JS minifier run for production mode.
es5-shim@4.6.15                  # ECMAScript 5 compatibility for older browsers.
ecmascript@0.6.1                 # Enable ECMAScript2015+ syntax in app code.

browser-policy@1.0.9             # Manage browser policies
kadira:flow-router               # Carefully Designed Client Side Router for Meteor.
kadira:blaze-layout              # Blaze Layout Manager for Meteor.
fourseven:scss                   # Sass and SCSS support for Meteor.
aldeed:simple-schema             # Simple, reactive schema validation smart package for Meteor.
aldeed:collection2               # Provide support for specifying and validating Mongo against a schema.
gwendall:body-events             # Get Template.body.events() working.
http                             # Provides an HTTP request API on the client and server.
session                          # Provides a global object on the client that you can use as a store.
